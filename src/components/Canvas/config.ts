export const GRID_LAYOUT_CONFIG = {
    cols: 24,           // 网格列数
    rowHeight: 30,      // 每行高度（像素）
    margin: [10, 10],   // 组件间距 [x, y]
    containerPadding: [10, 10], // 容器内边距 [x, y]
    minW: 2,           // 最小宽度（网格单位）
    minH: 2,           // 最小高度（网格单位）
    maxW: 24,          // 最大宽度（网格单位）
    maxH: 36,          // 最大高度（网格单位）
  };